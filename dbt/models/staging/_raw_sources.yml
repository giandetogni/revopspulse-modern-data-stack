version: 2

sources:
  - name: raw_warehouse
    description: RAW warehouse tables loaded from JSONL extracts.
    database: revopspulse
    schema: raw
    tables:
      - name: json_records
        description: Generic JSONB landing table for API and product event records.
        loaded_at_field: loaded_at
